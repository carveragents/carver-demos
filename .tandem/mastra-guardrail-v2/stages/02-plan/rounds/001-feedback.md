---
verdict: CHANGES_REQUESTED
round: 1
---

## Issues

1. **The plan violates the zero-billed Phases 0–6 contract and the “one late real run” rule twice.** P6.11 makes a live billed call while Phase 6 is labelled zero-billed, and R7.2 schedules a paid tiny-cap `run_prep.py` before R7.3's paid full run. Move the live containment spike to the start of Phase 7 as a named preflight (after the zero-cost template gate, before any corpus probing), and keep its stop-on-propagation behavior. Delete the paid tiny-cap prep run: prompt/corpus shakeout must use stubs/synthetic fixtures, and the real scenario-trial + curation run happens once. Update the phase map, Bills statements, zero-spend proof, stress-scenario assignment, preconditions, and cost text together. Also note that the proposed `scenario_trial_size: 2` conflicts with the shipped `scenario_trial_min: 10` validation unless another locked setting is changed; deleting the dry run removes that divergence.

2. **Phase 6's implementation order contradicts the approved TypeScript DAG.** P6.3 builds `judge/callJudge.ts` before `judgeAgent` exists even though it imports that agent; P6.4 builds `guardedAgent` before `CarverGuardrail` exists even though the agent imports the processor. Reorder/split tasks to the actual chain: config/schema/shared config → `judge/contract.ts` → baseline + judge agents → `judge/callJudge.ts` → narrowing + containment → `CarverGuardrail` → guarded agent → workflows → evals/report/Mastra. Move tests to the first task where their imports exist. The phase must end green at each claimed checkpoint, not with modules importing future files.

3. **The “hand-written stand-in” `scenario/prompts.ts` violates the spec's generated-only contract.** P6.9b says to hand-author a stand-in until P8.2, while the approved spec says this module is generated in full and never hand-authored. Once P3's generator exists, run `emit_template_config` against a synthetic cleared set plus synthetic scenario decision during Phase 6, producing all four generated TS fragments mechanically (`DEMO_TRIGGER_RECORD_ID`, firm profile, persona instructions, and `scenario/prompts.ts`). Build and test the synthetic template against those outputs with no skipped “generation has not run” cases. In P8, rerun the same generator against reviewed real data and rerun the same tests. This also means the template track may start in parallel, but its generated-source/test gate depends on P3; state that dependency rather than claiming P6 is wholly independent of P1–P5.

4. **Several parallelism claims are false.** The plan says all Phase-1 leaf tasks are mutually independent, then explicitly says P1.1's cutoff validation calls P1.4's `assert_cutoff_margin`. Track B also consumes fixtures created by P1.9 and P6.12 edits/reads `prep/tests/test_config.py` and prep constants. Update the graph and parallelism table: identify only the subtracks that can start independently, add joins on P1.9/P3 as applicable, and do not describe the whole P6 track or all P1 tasks as independent. Rubric 13 rejects exactly this kind of shared-state/order overlap.

5. **The Python commands are not executable under the working-directory rule as written.** The plan repeatedly says commands run “from `prep/`” but invokes `prep/.venv/bin/python`; from that directory this resolves to `prep/prep/.venv/bin/python`. Pin one unambiguous form everywhere, preferably `cd prep && .venv/bin/python -m pytest ...` / `cd prep && .venv/bin/python run_prep.py ...` (the interpreter is still the project-local `prep/.venv`). Apply it to setup verification, every phase gate, real-run commands, review/generation/verification commands, and DoD. Do not mix an implied prep cwd with root-relative test/script paths.

6. **The Python review cadence weakens an explicit repo convention.** P1.14 acknowledges “after each Python change” and then substitutes one review per phase boundary; P2–P4 repeat that batching. The task/rubric requires `python-code-reviewer` after each Python change with `python-expert` fixing findings. Put the review/fix checkpoint into each Python implementation task (or define a small repeated substep after every commit-sized Python change), then use phase gates only as an additional aggregate review. Do not reinterpret “after each” as “once per phase.”

7. **The citation DoD adds an automated behavior the approved spec explicitly excludes.** Criterion 8 says `run_prep.py --verify-cleared` re-resolves every URL, then cites the spec's statement that post-clearing automated re-validation is out of scope. In the approved spec, citations are validated at clearing time only; `--verify-cleared` validates cleared-record/sign-off structure, not a new post-clearing URL crawler. Prove criterion 8 from the review/clearance run's recorded `"resolves"` result and the review gate, with a manual spot/open observation if desired; keep the README limitation that later link death is a manual fix. Do not silently expand the CLI contract.

8. **Create the work branch before making project edits.** P0.5 currently follows P0.1–P0.4, so the skeleton, root README, dependency files, and installation artifacts are created before the required work branch. Move the branch task to P0.1/preflight, verify the current branch, then scaffold and commit incrementally; never push.

## Notes

The plan otherwise has strong module/test/stress-scenario coverage, explicitly preserves the n=30 negative control and corrected 609-call/$23 eval estimate, includes both human checkpoints, maps the nine success criteria, and carries the restatement discipline forward well.
