# Session: feat-showcase-v1
**2026-06-09 01:16**

## Overview
- **Start time:** 2026-06-09 01:16
- **Worktree:** /Users/achintthomas/work/scribble/code/repos/carver/carver-demos/.claude/worktrees/feat-showcase-v1
- **Session goal:** First version of showcase

## Goals
1. Build the initial version of the Carver Data Showcase application
2. Create a working demo that showcases the annotation dataset

## Progress

### v1 SHOWCASE — COMPLETE (2026-06-09)

Built v1 of the Carver Annotation Data Showcase: **two Streamlit prototypes over one shared
deterministic pipeline** (no LLM calls), from a real **58,982-record category-stratified**
snapshot pulled via the direct Artifacts API. 218 tests passing; both apps visually verified.

**Process (per goal):** brainstorming → **tandem** maker/checker/orchestrator pipeline for
spec+plan (`.tandem/carver-showcase/`, verdict COMPLETE: 01-spec approved over 3 rounds /
2 stress-test cycles closing 5 gaps; 02-plan approved) → **subagent-driven TDD** for
implementation (Sonnet implementers + Opus code-quality reviews per phase + final holistic
review). User ran the tandem maker/checker in separate sessions; I drove the orchestrator.

**Deliverables (`carver-data-showcase/`):**
- `carver_showcase/` — shared pipeline: `config, schema, ingest, normalize, load, metrics,
  richness, quality` (deterministic richness score + 9 quality predicates + 11 anomaly rules).
- `apps/gallery.py` — external **Showcase Gallery** (11 views: overview/KPIs/historical-depth,
  geography choropleth, monitored-institutions catalog, category sunburst, update-types, volume,
  score distributions, urgency basis, calibration, single-record drill-down, highlight reel).
- `apps/cockpit.py` — internal **Data-Quality Cockpit** (7 views: coverage matrix, cleanup
  queue+CSV, anomaly panel, field-health/cardinality, distributions, coverage trend, migration
  tracker).
- `apps/components/` — shared filters + render helpers. `tests/` — 218 tests.
- `tools/` — `pull_stratified.py` (stratified pull), `pull_topic_catalog.py` (1,071 institutions),
  `coverage_probe.py`, `probe_api.py`.
- `docs/v2-llm-enrichment-ideas.md` — 10 LLM-deferred ideas (no LLM in v1, per MUST-NOT-DO).
- `README.md`, `requirements.txt`.

**Key findings** (detail in `docs/LESSONS.md`): offset walk is ~99% Finance (MD=0) → needed a
category-stratified `topic_ids_in` pull; category recovered from the catalog endpoints (DP/MD ⊂
Finance → most-specific assignment); FIELD_MAP path corrections (title/feed_url/summary/language
under `classification.metadata`; `reconciled_published_date.date`); garbage dates (1947…2105);
`jurisdiction_tier` residual 5.1%.

**Run:** `.venv/bin/python -m streamlit run apps/gallery.py` / `… apps/cockpit.py`;
tests `.venv/bin/python -m pytest -q`. Snapshot is git-ignored — regenerate via the `tools/` pullers.

**Post-review polish (done):** swept `use_container_width=True` → `width="stretch"` (40 sites);
added `apps/components/theme.py` (shared `SCORE_AXES` + unified `AXIS_COLORS`) used by both apps;
promoted `metrics._plausible_mask/_ensure_utc` → `plausible_date_mask`/`ensure_utc` and
`quality._canonicalize_regulator` → `canonicalize_regulator` (public aliases) so the apps no
longer import privates. 218 tests still green; both apps visually re-verified.

**Not done:** not committed/merged (left for the flux commit/merge workflow).

